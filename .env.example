# GraphQL API Configuration (Hygraph)
GQL_HOST=https://api-region.hygraph.com/v2/your-project-id/master
GQL_TOKEN=your_hygraph_permanent_auth_token_here

# Cache Revalidation Secret (generate with: openssl rand -base64 32)
REVALIDATE_SECRET=your_random_secret_here
