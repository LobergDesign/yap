query GetProject($slug: String!) {
  project(where: { slug: $slug }) {
    id
    title
    subHeader
    slug
    intro {
      html
      text
    }
    createdDate
    websiteUrl
    npmUrl
    githubUrl
    codepenUrl
    projectCaseType
    createdAt
    updatedAt
  }
}
